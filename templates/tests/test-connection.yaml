apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cartadeployment.fullname" . }}-test-connection"
  labels:
    {{- include "cartadeployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cartadeployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
