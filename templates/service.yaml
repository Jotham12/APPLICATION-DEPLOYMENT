apiVersion: v1
kind: Service
metadata:
  name: {{ include "cartadeployment.fullname" . }}
  labels:
    {{- include "cartadeployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cartadeployment.selectorLabels" . | nindent 4 }}
